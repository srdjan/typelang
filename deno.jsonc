{
  "$schema": "https://deno.land/x/deno/schemas/deno.schema.json",
  "version": "0.4.0",
  "tasks": {
    "dev": "deno run -A server/main.ts",
    "dev:showcase": "deno run -A examples/showcase/main.ts",
    "dev:example": "deno run -A scripts/dev.ts",
    "test": "deno test --allow-read --allow-write",
    "test:watch": "deno test --allow-read --allow-write --watch",
    "test:coverage": "deno test --allow-read --allow-write --coverage=coverage && deno coverage coverage",
    "lint": "deno lint --quiet && deno run -A scripts/lint_subset.ts",
    "fmt": "deno fmt",
    "setup-hooks": "git config core.hooksPath .githooks && echo 'Git hooks configured!'"
  },
  "lint": {
    "rules": {
      "include": ["recommended"],
      "exclude": [
        "no-import-prefix",
        "ban-types",
        "no-explicit-any",
        "require-await",
        "no-unused-vars",
        "prefer-const"
      ]
    }
  },
  "fmt": { "useTabs": false, "lineWidth": 100, "indentWidth": 2, "semiColons": true },
  "imports": {
    "@srdjan/bluesky-bot": "jsr:@srdjan/bluesky-bot@^1.4.0"
  }
}
